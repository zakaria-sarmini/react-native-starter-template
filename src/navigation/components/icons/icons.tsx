import React from 'react';
import { Icon } from 'native-base';

export const HomeIcon: React.FC = () => {
	return <Icon type="Ionicons" name="home" />;
};

export const MenuIcon: React.FC = () => {
	return <Icon type="Ionicons" name="home" />;
};
